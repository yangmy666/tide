<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yangmy.tide.service.system.mapper.QuestionMapper">

    <select id="recommend" resultType="org.yangmy.tide.service.system.entity.vo.QuestionVo">
        SELECT q.id,q.question,q.like,su.username questioner FROM `question` q
            LEFT JOIN sys_user su on q.questioner_id=su.id and su.del_flag=0
        where q.del_flag=0
    </select>
</mapper>
